import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import * as React from 'react';
import styled from 'styled-components';
import validProp from '@emotion/is-prop-valid';
const StyledIcon = React.forwardRef((props, ref) => {
  const title = props.title,
        size = props.size,
        otherProps = _objectWithoutProperties(props, ["title", "size"]);

  const iconProps = {
    viewBox: '0 0 24 24',
    height: props.height !== undefined ? props.height : size,
    width: props.width !== undefined ? props.width : size,
    // @ts-ignore - aria is not always defined on SVG in React TypeScript types
    'aria-hidden': title == null ? 'true' : undefined,
    focusable: 'false',
    role: title != null ? 'img' : undefined,
    "fill": "currentColor"
  };
  const svgProps = Object.keys(otherProps).reduce((p, k) => {
    if (validProp(k)) {
      p[k] = otherProps[k];
    }

    return p;
  }, {});
  return React.createElement("svg", _extends({}, iconProps, svgProps, {
    ref: ref
  }), title && React.createElement("title", {
    key: "Battery-title"
  }, title), React.createElement("path", {
    d: "M20 7a1 1 0 0 0-1-1H4c-1.103 0-2 .897-2 2v8c0 1.103.897 2 2 2h15a1 1 0 0 0 1-1v-3h2v-4h-2V7zm-2 9H4V8h14v8z",
    key: "k0"
  }));
});
export const Battery = styled(StyledIcon).withConfig({
  displayName: "Battery",
  componentId: "ggpajl-0"
})(["display:inline-block;vertical-align:middle;overflow:hidden;"]);
Battery.displayName = 'Battery';
export const BatteryDimensions = {
  height: 24,
  width: 24
};